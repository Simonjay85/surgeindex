import CreatorsPage from "../creators/page";

export const metadata = { title: "Fanward", robots: { index: false, follow: false } };

export default function FanwardPage() {
  return <CreatorsPage />;
}
